name: multi env deploy

on:
  push:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: print env
      run: echo ${{ vars.ENVIRONMENT_NAME }}
